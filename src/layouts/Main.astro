---
import Head from "./Head.astro";
import "../styles/global.css";

const { title = "Portfolio", description = "Sadia Afrin's portfolio" } = Astro.props;
---

<Head {title} {description} />
<body>
  <div id="wrapper" class="hidden md:block">
    <div id="cursor"></div>
  </div>
  <main id="main" class="border-2 border-colBorder m-3 md:w-[75%] w-[95%] mx-auto">
    <slot />
  </main>
</body>

<style>
  #cursor {
    width: 400px;
    height: 400px;
    border-radius: 100%;
    /* border: 1px solid black; */
    background-color: #3838be1c;
    filter: blur(70px);
    position: fixed;
    pointer-events: none;
    left: 0;
    top: 0;
    transform: translate(calc(-50% + 15px), -50%);
    z-index: 2;
    transition: all 0.2s ease-out;
  }

  #main {
    background: var(--colorBg);
  }
</style>

<script>
  const cursor = document.getElementById("cursor");
  const cursorStyle = cursor.style;

  document.addEventListener("mousemove", (e) => {
    cursorStyle.transform = `translate3d(calc(${e.clientX}px - 50%), calc(${e.clientY}px - 50%), 0)`;
  });
</script>
