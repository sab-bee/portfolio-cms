---
import { renderRichText, storyblokEditable } from "@storyblok/astro";
import ExperienceSlot from "../components/ExperienceSlot.svelte";

const { blok, counter } = Astro.props;
const renderTask = renderRichText(blok.tasks);
---

<ExperienceSlot {...storyblokEditable(blok)} blok={blok} renderTask={renderTask} client:load />
