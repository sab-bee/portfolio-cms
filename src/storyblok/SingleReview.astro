---
import { storyblokEditable } from "@storyblok/astro";
import ReviewCard from "../components/ReviewCard.svelte";

const { blok } = Astro.props;
---

<ReviewCard {...storyblokEditable(blok)} blok={blok} client:load />
